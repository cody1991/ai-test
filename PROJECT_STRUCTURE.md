# 📁 项目结构

```
ai-test/
│
├── 📄 README.md                  # 项目说明
├── 📄 DEMO.md                    # 快速演示
├── 📄 package.json               # 项目脚本
│
├── 📁 web/                       # Web 应用
│   ├── src/
│   │   ├── pages/               # 页面组件
│   │   │   ├── Dashboard.tsx
│   │   │   ├── MenuManagement.tsx
│   │   │   ├── OrderManagement.tsx
│   │   │   ├── ReservationManagement.tsx
│   │   │   └── InventoryManagement.tsx
│   │   ├── layouts/
│   │   │   └── MainLayout.tsx
│   │   ├── App.tsx
│   │   └── index.tsx
│   ├── public/
│   │   └── index.html
│   ├── package.json
│   ├── rspack.config.ts
│   └── tsconfig.json
│
├── 📁 specs/                     # Spec-kit 规则
│   ├── README.md                # Spec 使用说明
│   └── restaurant.spec.ts       # 餐厅系统测试规则
│
└── 📁 tests/                     # 测试系统
    ├── spec-generator.ts        # 测试用例生成器
    ├── runner.ts                # 测试执行器
    └── generated/               # AI 生成的测试用例
        ├── test-cases.json      # 测试用例（JSON）
        └── *.mcp.txt            # MCP 执行脚本
```

## 核心文件说明

### 📁 web/ - Web 应用

**意大利餐厅管理系统**，包含 5 个页面：
- 仪表盘（数据概览）
- 菜单管理（增删改查）
- 订单管理（订单处理）
- 预订管理（餐位预订）
- 库存管理（食材库存）

### 📁 specs/ - Spec-kit 规则

**测试规则配置**，定义测试范围：
- `restaurant.spec.ts` - 餐厅系统的测试规则
- 定义页面、功能、操作、验证规则

### 📁 tests/ - 测试系统

**AI 测试引擎**：
- `spec-generator.ts` - 根据 Spec 规则生成测试用例
- `runner.ts` - 使用 MCP 执行测试
- `generated/` - AI 自动生成的测试文件

## 工作流程

```
1. 开发人员编写 Spec 规则
   ↓
2. 运行 npm run spec "测试需求"
   ↓
3. AI 分析 Spec + 生成测试用例
   ↓
4. 保存到 tests/generated/
   ↓
5. 运行 npm test 执行测试
```

## 添加新项目测试

### 步骤 1: 创建 Spec 规则

```typescript
// specs/your-project.spec.ts
export const yourProjectSpec = [
  {
    name: '登录页',
    path: '/login',
    features: [
      {
        name: '用户登录',
        type: 'form',
        actions: [...],
        validations: [...]
      }
    ]
  }
];
```

### 步骤 2: 修改生成器

```typescript
// tests/spec-generator.ts
import { yourProjectSpec } from '../specs/your-project.spec.js';

// 将 restaurantSpec 替换为 yourProjectSpec
```

### 步骤 3: 生成测试

```bash
npm run spec "测试登录功能"
```

## 文件大小统计

- **Web 应用**: ~10KB (5个页面)
- **Spec 规则**: ~6KB (1个规则文件)
- **测试系统**: ~11KB (2个核心文件)
- **总计**: ~27KB

**极简架构，专注核心功能！**
