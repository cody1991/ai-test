# ⚡ 5分钟快速上手

## 🎯 核心理念

**一句话 → AI生成测试 → 自动执行**

不需要写死测试用例，只需定义规则（Spec），AI 自动生成！

## 🚀 三步开始

### 1️⃣ 生成测试用例

```bash
npm run spec "测试菜单管理的添加功能"
```

**输出**：
```
✅ 生成了 1 个测试用例
1. 菜单管理 - 添加菜品
   步骤数: 6
   验证项: 3

💾 已保存到: tests/generated/
```

### 2️⃣ 查看生成的测试

```bash
cat tests/generated/test-cases.json
```

你会看到完整的测试步骤、选择器、输入值、预期结果。

### 3️⃣ 执行测试

```bash
npm test
```

提示你使用 Chrome DevTools MCP 执行测试。

## 🔥 更多示例

### 测试单个功能
```bash
npm run spec "测试订单的添加功能"
npm run spec "测试用户登录"
npm run spec "测试菜品的编辑和删除"
```

### 测试整个页面
```bash
npm run spec "测试菜单管理"
npm run spec "测试订单管理"
```

### 测试所有页面
```bash
npm run spec "测试所有页面"
```

**一次生成 13 个测试用例！**

## 📖 核心文件

| 文件 | 作用 |
|------|------|
| `specs/restaurant.spec.ts` | 测试规则配置 |
| `tests/spec-generator.ts` | AI 生成器 |
| `tests/generated/` | 生成的测试用例 |

## 💡 理解 Spec-kit

### Spec 是什么？

**测试规则模板**，定义"测什么"和"怎么测"。

```typescript
{
  name: '菜单管理',              // 页面名称
  path: '/menu',                // 页面路径
  features: [                   // 功能列表
    {
      name: '添加菜品',          // 功能名称
      type: 'form',             // 功能类型
      actions: [                // 操作步骤
        { name: '打开表单', selector: 'button', expected: '弹出表单' },
        { name: '填写名称', selector: 'input', input: '测试', expected: '输入成功' }
      ],
      validations: [            // 验证规则
        { field: 'name', rules: ['required'], errorMessage: '请输入名称' }
      ]
    }
  ]
}
```

### AI 如何生成测试？

1. **读取 Spec 规则** - 了解页面结构和功能
2. **分析用户需求** - 匹配相关页面和功能
3. **生成测试步骤** - 根据 actions 生成操作序列
4. **添加验证逻辑** - 根据 validations 生成断言
5. **输出可执行代码** - 生成 MCP 命令

## 🎨 自定义测试

### 添加新页面测试

1. 编辑 `specs/restaurant.spec.ts`
2. 添加新页面配置
3. 运行 `npm run spec "测试新页面"`

示例：
```typescript
{
  name: '用户设置',
  path: '/settings',
  features: [
    {
      name: '修改密码',
      type: 'form',
      actions: [
        { name: '填写旧密码', selector: '#oldPassword', input: '123', expected: '输入成功' },
        { name: '填写新密码', selector: '#newPassword', input: '456', expected: '输入成功' },
        { name: '确认密码', selector: '#confirmPassword', input: '456', expected: '输入成功' },
        { name: '提交', selector: 'button[type="submit"]', expected: '修改成功' }
      ],
      validations: [
        { field: 'oldPassword', rules: ['required'], errorMessage: '请输入旧密码' },
        { field: 'newPassword', rules: ['required', 'minLength:6'], errorMessage: '密码至少6位' }
      ]
    }
  ]
}
```

## 🌟 实战技巧

### 技巧 1: 精确匹配

```bash
# 精确测试某个功能
npm run spec "测试添加功能"  # 会匹配所有"添加"功能

# 限定范围
npm run spec "测试菜单管理的添加功能"  # 只匹配菜单管理的添加
```

### 技巧 2: 批量生成

```bash
# 一次性生成多个测试
npm run spec "测试所有页面"

# 生成结果
✅ 生成了 13 个测试用例
涵盖 5 个页面的所有功能
```

### 技巧 3: 查看生成结果

```bash
# JSON 格式（程序可读）
cat tests/generated/test-cases.json

# MCP 脚本（人类可读）
cat tests/generated/*.mcp.txt
```

## ❓ 常见问题

### Q: Spec 和测试用例有什么区别？

**A:** 
- **Spec** = 规则模板（写一次）
- **测试用例** = 可执行代码（AI自动生成）

### Q: 需要学习新的语法吗？

**A:** 不需要！Spec 是简单的 TypeScript 对象，非常直观。

### Q: 如何调试测试？

**A:** 
1. 查看 `tests/generated/test-cases.json` 确认测试步骤
2. 查看 `tests/generated/*.mcp.txt` 确认 MCP 命令
3. 手动运行 MCP 工具逐步执行

## 🎉 开始使用

```bash
# 1. 生成测试
npm run spec "你的测试需求"

# 2. 查看结果
cat tests/generated/test-cases.json

# 3. 执行测试
npm test
```

---

**开始你的智能化测试之旅！** 🚀

📚 更多信息: 
- 完整说明: [README.md](./README.md)
- 演示示例: [DEMO.md](./DEMO.md)
- Spec 文档: [specs/README.md](./specs/README.md)
