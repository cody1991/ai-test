# 🎯 快速演示

## 核心流程

**一句话生成测试用例，不用写死代码！**

```bash
# 1️⃣ 生成测试用例
npm run spec "测试菜单管理的添加功能"

# 2️⃣ 查看生成的测试
cat tests/generated/test-cases.json

# 3️⃣ 执行测试
npm test
```

## 演示 1: 单个功能测试

```bash
npm run spec "测试菜单管理的添加功能"
```

**输出**:
```
✅ 生成了 1 个测试用例

1. 菜单管理 - 添加菜品
   页面: 菜单管理
   功能: 添加菜品
   步骤数: 6
   验证项: 3
```

**生成的 MCP 命令** (`tests/generated/menu_添加菜品.mcp.txt`):
```javascript
mcp.navigate_page("http://localhost:3000/menu")
mcp.click("button:contains('添加菜品')")
mcp.fill("input[id*='name']", "测试菜品")
mcp.fill("input[id*='price']", "99")
mcp.click(".ant-select")
mcp.click("button[type='submit']")

// 验证规则:
// - name: required, maxLength:50
// - price: required, number, min:0
// - category: required
```

## 演示 2: 批量测试所有页面

```bash
npm run spec "测试所有页面"
```

**输出**:
```
✅ 生成了 13 个测试用例

1. 仪表盘 - 数据卡片展示
2. 菜单管理 - 菜品列表
3. 菜单管理 - 添加菜品
4. 菜单管理 - 编辑菜品
5. 菜单管理 - 删除菜品
6. 订单管理 - 订单列表
7. 订单管理 - 订单状态更新
... (共13个)
```

一次生成 **13 个完整测试用例**！

## 演示 3: 多功能组合测试

```bash
npm run spec "测试订单的添加和编辑功能"
```

AI 会：
1. 分析需求中的"订单"、"添加"、"编辑"关键词
2. 匹配 `order.spec.ts` 中的相关功能
3. 生成多个测试用例覆盖不同场景

## 对比：传统 vs Spec-kit

### ❌ 传统方式（写死测试）

```typescript
// 需要手写每个测试用例
test('添加菜品', async () => {
  await page.goto('/menu');
  await page.click('button:contains("添加菜品")');
  await page.fill('#name', '测试菜品');
  await page.fill('#price', '99');
  await page.selectOption('#category', '主菜');
  await page.click('button[type="submit"]');
  expect(await page.textContent('.message')).toContain('成功');
});

// 测试编辑...（重复写代码）
// 测试删除...（重复写代码）
// 测试列表...（重复写代码）
```

**问题**:
- 每个功能都要手写测试代码
- 页面改动需要修改所有测试
- 维护成本高

### ✅ Spec-kit 方式

```typescript
// 只需定义一次规则 (specs/restaurant.spec.ts)
{
  name: '菜单管理',
  features: [
    {
      name: '添加菜品',
      actions: [...],
      validations: [...]
    }
  ]
}

// 然后一句话生成所有测试
npm run spec "测试菜单管理"
```

**优势**:
- 规则定义一次，自动生成测试
- AI 理解业务逻辑，覆盖更全面
- 页面改动只需更新 Spec

## 实际使用场景

### 场景 1: 日常开发

```bash
# 开发新功能后
npm run spec "测试用户注册功能"

# 立即运行测试
npm test
```

### 场景 2: 回归测试

```bash
# 发版前测试所有核心功能
npm run spec "测试所有页面"

# 生成完整测试报告
npm test
```

### 场景 3: 针对性测试

```bash
# 修复 Bug 后验证
npm run spec "测试登录的错误提示"

# 或测试特定流程
npm run spec "测试从下单到支付的完整流程"
```

## 查看生成的测试用例

```bash
# JSON 格式（结构化数据）
cat tests/generated/test-cases.json

# MCP 脚本（可执行命令）
cat tests/generated/*.mcp.txt
```

## 下一步

1. **启动 Web 应用**: `npm run web:dev`
2. **生成测试用例**: `npm run spec "你的测试需求"`
3. **执行测试**: `npm test`

---

**让 AI 为你生成测试，专注于业务开发！** 🚀
