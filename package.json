{
  "name": "ai-test-automation",
  "version": "1.0.0",
  "description": "AI-powered automation testing system with Spec-kit",
  "scripts": {
    "web:dev": "cd web && npm run dev",
    "web:build": "cd web && npm run build",
    "test": "tsx tests/runner.ts",
    "spec": "tsx tests/spec-generator.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "@types/node": "^22.5.0"
  }
}
