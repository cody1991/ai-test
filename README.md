# AI 自动化测试系统

基于 **Spec-kit** 规则和 **Chrome DevTools MCP** 的智能测试系统。

## 🎯 核心特性

- ✅ **不写死测试用例** - 通过 Spec 规则，AI 动态生成测试
- ✅ **大规模测试** - 一句话生成多个页面的完整测试套件
- ✅ **智能化执行** - 使用 MCP 工具自动化浏览器操作
- ✅ **易于维护** - 只需维护 Spec 规则，测试代码自动生成

## 📁 项目结构

```
ai-test/
├── web/                    # Web 应用（餐厅管理系统）
│   ├── src/               # React 源码
│   ├── public/            # 静态资源
│   └── package.json       # Web 依赖
│
├── specs/                  # Spec-kit 规则配置
│   ├── restaurant.spec.ts # 餐厅系统测试规则
│   └── README.md          # Spec 使用说明
│
├── tests/                  # 测试系统
│   ├── spec-generator.ts  # 测试用例生成器
│   ├── runner.ts          # 测试执行器
│   └── generated/         # AI 生成的测试用例
│
└── package.json           # 项目脚本
```

## 🚀 快速开始

### 1. 安装依赖

```bash
# 根目录安装
npm install

# Web 应用安装
cd web && npm install
```

### 2. 启动 Web 应用

```bash
npm run web:dev
```

访问 http://localhost:3000

### 3. 生成测试用例

```bash
# 测试菜单管理
npm run spec "测试菜单管理"

# 测试所有页面
npm run spec "测试所有页面"

# 测试特定功能
npm run spec "测试订单的添加和编辑功能"
```

### 4. 执行测试

```bash
npm test
```

## 🎓 使用示例

### 场景 1: 测试登录流程

**传统方式**（需要写死测试代码）：
```typescript
test('登录测试', async () => {
  await page.goto('/login');
  await page.fill('#username', 'admin');
  await page.fill('#password', '123456');
  await page.click('#submit');
  // ... 更多代码
});
```

**Spec-kit 方式**（只需一句话）：
```bash
npm run spec "测试登录流程"
```

AI 会：
1. 读取 `login.spec.ts` 规则
2. 自动生成 5-10 个测试用例（空值、错误凭证、正确登录等）
3. 生成可执行的 MCP 命令
4. 你只需运行 `npm test`

### 场景 2: 大型功能测试

**你开发了一个复杂的订单管理模块**：
- 订单列表（分页、筛选、排序）
- 创建订单（表单验证、金额计算）
- 订单详情（状态流转、备注）
- 订单导出（Excel、PDF）

**传统方式**：需要写 50+ 个测试用例

**Spec-kit 方式**：
```bash
# 1. 定义 Spec 规则（一次性）
编辑 specs/order.spec.ts

# 2. 生成测试
npm run spec "测试订单管理的所有功能"

# 3. 执行
npm test
```

AI 自动生成覆盖所有场景的测试用例！

## 📖 Spec-kit 规则示例

```typescript
// specs/restaurant.spec.ts
export const restaurantSpec = [
  {
    name: '菜单管理',
    path: '/menu',
    description: '菜品的增删改查',
    features: [
      {
        name: '添加菜品',
        type: 'form',
        actions: [
          { name: '打开表单', selector: 'button:contains("添加")', expected: '弹出表单' },
          { name: '填写名称', selector: 'input[id*="name"]', input: '测试菜品', expected: '输入成功' },
          { name: '提交表单', selector: 'button[type="submit"]', expected: '添加成功' }
        ],
        validations: [
          { field: 'name', rules: ['required'], errorMessage: '请输入菜品名称' },
          { field: 'price', rules: ['required', 'number'], errorMessage: '请输入有效价格' }
        ]
      }
    ]
  }
];
```

## 🔧 工作流程

```
┌─────────────┐
│  你的需求   │  "测试菜单管理"
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Spec 规则   │  读取 restaurant.spec.ts
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ AI 生成器   │  生成 10 个测试用例
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ MCP 执行    │  自动化浏览器操作
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 测试报告    │  截图 + 日志 + 结果
└─────────────┘
```

## 🛠️ 技术栈

- **前端**: React 18 + Rspack + Ant Design
- **测试协议**: MCP (Model Context Protocol)
- **浏览器控制**: Chrome DevTools Protocol
- **AI**: 通过 Cursor/Windsurf 等 AI 编程助手

## 📚 更多文档

- [Spec-kit 详细说明](./specs/README.md)
- [如何编写 Spec 规则](./specs/README.md#规则定义示例)

## ❓ 常见问题

### Q1: Spec-kit 和测试用例有什么区别？

**Spec-kit** 是规则模板，定义"测什么"  
**测试用例** 是可执行代码，定义"怎么测"

你只需维护 Spec，AI 自动生成测试用例。

### Q2: 需要 GPT-4 API Key 吗？

不需要！MCP 是免费工具，你已经通过 AI 编程助手订阅了 AI 能力。

### Q3: 如何添加新页面的测试？

```typescript
// 1. 在 specs/restaurant.spec.ts 添加配置
{
  name: '新页面',
  path: '/new-page',
  features: [...]
}

// 2. 生成测试
npm run spec "测试新页面"
```

### Q4: 测试失败了怎么办？

AI 会生成详细的测试报告，包括：
- 失败截图
- 网络请求日志
- 控制台错误
- 期望 vs 实际结果对比

## 🎉 开始使用

```bash
# 1. 生成测试
npm run spec "测试菜单管理的添加功能"

# 2. 查看生成的测试用例
cat tests/generated/test-cases.json

# 3. 执行测试
npm test
```

---

**让 AI 为你生成测试用例，专注于业务开发！** 🚀
